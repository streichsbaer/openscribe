# OpenScribe Release Guide

The canonical release runbook lives in [site-docs/ops/release.md](../site-docs/ops/release.md).

Use that page for:

- preflight verification
- unsigned app build steps
- signing and notarization
- GitHub Release publishing
- Homebrew cask updates

Current release builds bundle an architecture-matched `whisper-cli` inside `OpenScribe.app`.
